server:
  port: 8763

spring:
  application:
    name: feign

eureka:
  client:
    eureka-server-u-r-l-context: http://localhost:8761/eureka

#让熔断器hystrix生效，默认是禁用的
feign:
  hystrix:
    enabled: true